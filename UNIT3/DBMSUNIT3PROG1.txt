DECLARE
    a NUMBER;
    b NUMBER;
    c NUMBER;
BEGIN
    a := &a;
    b := &b;

    c := a / b;

    DBMS_OUTPUT.PUT_LINE('Result = ' || c);

EXCEPTION
    WHEN ZERO_DIVIDE THEN
        DBMS_OUTPUT.PUT_LINE('Error: Cannot divide by zero.');
END;
/
